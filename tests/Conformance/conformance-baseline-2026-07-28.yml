# Every client and server scenario this revision's suite runs currently passes,
# so there is nothing to excuse. Keep the file: the runner is invoked with it,
# and a regression is supposed to show up as an unexpected failure rather than
# be quietly absorbed by a list nobody has pruned.
client: []
